<center>
<h2>ADD SUBACTEGORY</h2>
  <%= render 'form_subcategory', subcategory: @subcategory, parent_id: @category.id%>
</center>
<table class="table table-striped table-hover">
   <thead>
    <tr>
      <th scope="col"></th>
      <th scope="col">NAME</th>
      <th scope="col">Action</th>
      <th scope="col"></th>
    </tr>
   </thead>
<tbody>
    <% unless @category.parent_id.present? %>
    <tr>
          <td>CATEGORY</td>
          <td><%=@category.category_name %></td>
          <td></td>
          <td></td>
  </tr>
</tbody>
<tbody>
  <tr>
          <% @category.subcategories.each do |subcategorie| %>
          <td>SUBCATEGORY</td>
          <td><%=subcategorie.category_name %></td>
          <td><%= link_to "EDIT" ,edit_admin_category_path(subcategorie),class:"btn btn-primary btn-sm"%></td>
          <td><%= link_to "delete" ,admin_category_path(subcategorie), method: :delete,class:"btn btn-danger btn-sm",data: { confirm: "Are you sure?" }%></td>
    </tr>
</tbody>
        <%end%>
      </tr>
  <%end%>
</tbody>
</table>
